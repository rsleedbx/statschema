# CI test environment — offline tests only (no PySpark, no live databases)
#
# Spark-dependent tests auto-skip via pytest.importorskip("pyspark").
# Live-database tests auto-skip when the DB is not reachable.
#
# Used by:
#   .github/workflows/ci.yml

# ── Core runtime ─────────────────────────────────────────────────────────────
PyYAML>=6.0
python-dotenv>=1.0
sqlglot>=20.0.0

# ── Data generation (offline) ─────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.20.0

# ── Offline workload tests (TPC-DS, TPC-E) ───────────────────────────────────
duckdb>=0.9.0

# ── Test runner ───────────────────────────────────────────────────────────────
pytest>=7.0.0
pytest-cov>=4.0.0
