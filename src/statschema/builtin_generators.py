"""
Built-in column generators.

Public API (unchanged) — implementation lives in ``core/builtin_generators.py``.
"""

from __future__ import annotations

from .core.builtin_generators import (
    dispatch,
    _register,
    decorator,
)
