# Credentials have moved to the repo root.
# Copy /.env.example → /.env (at the repo root) and fill in your values.
# The notebooks load from the root .env automatically.
#
# See ../.env.example for the full list of variables.
