# CI / no-credentials test environment (local Spark, no Databricks Connect)
#
# Use this ONLY in CI pipelines or when no ~/.databrickscfg is available.
# Intentionally omits databricks-connect so SparkSession.builder.master("local[1]")
# creates an in-process Spark session instead of raising "remote sessions only".
#
# For LOCAL DEVELOPMENT, use .venv_3_11 with requirements-dev.txt instead:
#   .venv_3_11/bin/pip install -r requirements-dev.txt
#   .venv_3_11/bin/python -m pytest tests/ -v   # needs ~/.databrickscfg
#
# CI setup:
#   python -m venv .venv_test
#   .venv_test/bin/pip install -r requirements-test.txt
#   .venv_test/bin/python -m pytest tests/ -v

# Same as requirements.txt except databricks-connect (and pyspark is required)
PyYAML>=6.0
dbldatagen>=0.4.0
pyparsing
jmespath
databricks-zerobus-ingest-sdk>=0.3.0
databricks-sdk>=0.20.0
protobuf>=4.25.0,<7.0
grpcio-tools>=1.60.0,<2.0

# Local Spark for tests (must be present so Spark-dependent tests don't skip)
pyspark>=3.5.0

# dbldatagen import dependencies (not always declared by dbldatagen)
numpy>=1.20.0
pandas>=1.0.0

# Dev
pytest>=7.0.0
